/**
 * Error Boundary — Re-exports RecoverySystem for backward compatibility
 *
 * @deprecated Use RecoverySystem from '../lib/recovery/RecoverySystem' directly
 */
export { RecoverySystem, useRecoveryTrigger } from '../lib/recovery/RecoverySystem';
